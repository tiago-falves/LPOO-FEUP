package g24.model.element.projectile;

import g24.controller.commands.user.COMMAND;
import org.junit.Test;

import static junit.framework.TestCase.*;


public class BulletTest {
    @Test
    public void test() {
        /*Bullet bullet = new Bullet(2, 2);

        assertEquals(1, bullet.getDamage());
        assertEquals("*", bullet.getSymbol().getSymbol());

        assertEquals(bullet.exists(), false);
        bullet.setExists(true);
        assertEquals(bullet.exists(), true);

        assertEquals(COMMAND.UP, bullet.getDirection());
        bullet.setDirection(COMMAND.DOWN);
        assertEquals(COMMAND.DOWN, bullet.getDirection());

        bullet.shootDown();
        bullet.shootLeft();
        bullet.shootRight();
        bullet.shootUp();
        assertEquals(2, bullet.getPositions().getPositionList().get(0).getX());
        assertEquals(2, bullet.getPositions().getPositionList().get(0).getY());*/
    }
}
